{# ===== SUKOON Blog Page — /blogs/{handle} ===== #}
{% set blog = context.blog | default({}) %}
{% set articles = blog.articles | default([]) %}
{% set cols = widget.data.columns | default('3') %}
{% set displayLayout = widget.data.display_layout | default('grid') %}
{% set showDate = widget.data.show_date | default(true) %}
{% set showExcerpt = widget.data.show_excerpt | default(true) %}
{% set id = widget.id %}

{# Column grid classes #}
{% if cols == '2' %}
  {% set gridClass = 'grid-cols-1 sm:grid-cols-2' %}
{% elif cols == '4' %}
  {% set gridClass = 'grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4' %}
{% else %}
  {% set gridClass = 'grid-cols-1 sm:grid-cols-2 lg:grid-cols-3' %}
{% endif %}

<section id="{{ id }}" class="py-10 md:py-16">
  <div class="max-w-7xl mx-auto px-4 sm:px-6">

    {# ── Breadcrumbs ── #}
    <nav class="flex items-center gap-2 text-[13px] text-tertiary mb-8 flex-wrap">
      <a href="/" class="hover:text-primary transition-colors">{{ t('common.home') }}</a>
      <svg class="w-3 h-3 opacity-40 rtl:rotate-180" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/></svg>
      <a href="/blogs" class="hover:text-primary transition-colors">{{ t('pages.blog.title') | default('المدونة') }}</a>
      <svg class="w-3 h-3 opacity-40 rtl:rotate-180" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/></svg>
      <span class="text-text-primary font-medium">{{ blog.title | default('') }}</span>
    </nav>

    {# ── Blog Header ── #}
    <div class="mb-10 reveal">
      <h1 class="text-3xl md:text-4xl font-bold text-text-primary mb-3">{{ blog.title | default('') }}</h1>
      {% if blog.description %}
        <p class="text-tertiary text-base max-w-2xl leading-relaxed">{{ blog.description | striptags }}</p>
      {% endif %}
      {% if articles | length %}
        <div class="mt-4">
          <span class="inline-flex items-center gap-1.5 text-sm text-tertiary">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="1.5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M19 20H5a2 2 0 01-2-2V6a2 2 0 012-2h10a2 2 0 012 2v1m2 13a2 2 0 01-2-2V7m2 13a2 2 0 002-2V9a2 2 0 00-2-2h-2m-4-3H9M7 16h6M7 8h6v4H7V8z"/>
            </svg>
            {{ articles | length }} {{ t('pages.blog.articles_count') | default('مقال') }}
          </span>
        </div>
      {% endif %}
    </div>

    {# ── Articles ── #}
    {% if articles and (articles | length) %}

      {% if displayLayout == 'slider' %}
        {# ── Slider Layout ── #}
        <div class="reveal">
          <div class="swiper" id="swiper-{{ id }}">
            <div class="swiper-wrapper" style="padding-bottom: 4px;">
              {% for article in articles %}
                <div class="swiper-slide" style="height: auto;">
                  {% include "ui/article-card.njk" %}
                </div>
              {% endfor %}
            </div>
          </div>

          {# Navigation #}
          <div class="flex items-center justify-center gap-6 mt-10">
            <button id="prev-{{ id }}" aria-label="Previous"
                    class="group w-11 h-11 rounded-full flex items-center justify-center transition-all duration-300"
                    style="border: 1.5px solid var(--color-border); background: transparent;">
              <svg class="w-4 h-4 rtl:rotate-180 transition-transform duration-300 group-hover:-translate-x-0.5 rtl:group-hover:translate-x-0.5"
                   style="color: var(--color-text-secondary);" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24">
                <path d="M15 19l-7-7 7-7" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            </button>
            <div id="dots-{{ id }}" class="flex items-center gap-2"></div>
            <button id="next-{{ id }}" aria-label="Next"
                    class="group w-11 h-11 rounded-full flex items-center justify-center transition-all duration-300"
                    style="border: 1.5px solid var(--color-border); background: transparent;">
              <svg class="w-4 h-4 rtl:rotate-180 transition-transform duration-300 group-hover:translate-x-0.5 rtl:group-hover:-translate-x-0.5"
                   style="color: var(--color-text-secondary);" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24">
                <path d="M9 5l7 7-7 7" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            </button>
          </div>
        </div>

        <style>
          #prev-{{ id }}:hover, #next-{{ id }}:hover { border-color: var(--color-accent); background: var(--color-accent); }
          #prev-{{ id }}:hover svg, #next-{{ id }}:hover svg { color: #fff; }
          #dots-{{ id }} .dot { width: 24px; height: 3px; border-radius: 9999px; background: var(--color-border); transition: all 400ms ease; cursor: pointer; }
          #dots-{{ id }} .dot.active { width: 40px; background: var(--color-accent); }
        </style>

        <script>
          (() => {
            var cols = {{ cols }};
            var swiper = new Swiper('#swiper-{{ id }}', {
              slidesPerView: 1,
              spaceBetween: 20,
              grabCursor: true,
              loop: {{ articles | length }} > cols,
              navigation: { prevEl: '#prev-{{ id }}', nextEl: '#next-{{ id }}' },
              breakpoints: {
                640: { slidesPerView: 2, spaceBetween: 20 },
                1024: { slidesPerView: Math.min(cols, 3), spaceBetween: 24 },
                1280: { slidesPerView: cols, spaceBetween: 24 }
              }
            });

            var dotsC = document.getElementById('dots-{{ id }}');
            var total = {{ articles | length }};
            var pv = window.innerWidth >= 1280 ? cols : window.innerWidth >= 1024 ? Math.min(cols, 3) : window.innerWidth >= 640 ? 2 : 1;
            var dc = Math.max(total - pv + 1, 1);
            for (var i = 0; i < dc; i++) {
              var d = document.createElement('span');
              d.className = 'dot' + (i === 0 ? ' active' : '');
              d.dataset.index = i;
              d.addEventListener('click', function () { swiper.slideTo(parseInt(this.dataset.index)); });
              dotsC.appendChild(d);
            }
            swiper.on('slideChange', function () {
              var dots = dotsC.querySelectorAll('.dot');
              var idx = swiper.realIndex % dc;
              dots.forEach(function (dd, j) { dd.classList.toggle('active', j === idx); });
            });
          })();
        </script>

      {% else %}
        {# ── Grid Layout ── #}
        <div class="grid {{ gridClass }} gap-6">
          {% for article in articles %}
            <div class="reveal" style="transition-delay: {{ loop.index0 * 80 }}ms;">
              {% include "ui/article-card.njk" %}
            </div>
          {% endfor %}
        </div>
      {% endif %}

      {# ── Pagination ── #}
      {% if context.pagination and context.pagination.totalPages > 1 %}
        {% include "ui/pagination.njk" %}
      {% endif %}

    {% else %}
      {# ── Empty State ── #}
      <div class="text-center py-20 reveal flex items-center justify-center flex-col ">
        <div class="w-20 h-20 mx-auto mb-5 rounded-full bg-gray-50 flex items-center justify-center">
          <svg class="w-10 h-10 text-gray-200" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="1">
            <path stroke-linecap="round" stroke-linejoin="round" d="M19 20H5a2 2 0 01-2-2V6a2 2 0 012-2h10a2 2 0 012 2v1m2 13a2 2 0 01-2-2V7m2 13a2 2 0 002-2V9a2 2 0 00-2-2h-2m-4-3H9M7 16h6M7 8h6v4H7V8z"/>
          </svg>
        </div>
        <p class="text-base font-semibold text-text-primary mb-1">{{ t('pages.blog.no_articles') | default('لا توجد مقالات بعد') }}</p>
        <p class="text-sm text-tertiary mb-6">{{ t('pages.blog.no_articles_desc') | default('ستظهر المقالات هنا قريباً') }}</p>
        <a href="/blogs" class="inline-flex items-center gap-2 bg-accent text-white text-xs font-semibold px-6 py-2.5 rounded-full hover:opacity-90 transition-all">
          {{ t('pages.blog.back_to_blogs') | default('العودة للمدونات') }}
          <svg class="w-3.5 h-3.5 rtl:rotate-180" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
          </svg>
        </a>
      </div>
    {% endif %}

  </div>
</section>
